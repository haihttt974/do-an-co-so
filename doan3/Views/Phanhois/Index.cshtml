@model IEnumerable<doan3.Models.Phanhoi>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Noidung)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Thoigianph)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hocvien)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Noidung)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Thoigianph)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Hocvien.HocvienId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PhanhoiId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PhanhoiId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PhanhoiId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
