@model IEnumerable<doan3.Models.LoaiThi>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tenloaithi)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tenloaithi)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LoaithiId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LoaithiId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LoaithiId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
