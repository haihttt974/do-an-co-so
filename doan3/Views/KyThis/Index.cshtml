@model IEnumerable<doan3.Models.KyThi>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tenkythi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hang)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Loaithi)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tenkythi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Hang.HangId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Loaithi.LoaithiId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.KythiId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.KythiId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.KythiId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
